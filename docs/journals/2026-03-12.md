# Dev Journal — 2026-03-12

## myai Improvement Sprint

Completed 6 improvement tasks in a parallel cook session.

---

### gitignore-claude

`.claude/` was already in `.gitignore` from a previous session. No-op but confirmed the invariant: `.myai/` is source, `.claude/` is installed artifact and should never be tracked.

---

### restore-verify

Restored `/myai:verify` slash command — was removed during an earlier refactor when verify/validate naming got confused. Wrote `myai:verify.md` which spawns the `verifier` agent for goal-backward phase verification. Wired it back into `CLAUDE.md` and `README.md`. Kept the distinction clean: validate = before build (plan interview), verify = after build (goal check against ROADMAP).

Task 5 (propagate to smw) was skipped — out of scope for this repo.

---

### tpai-to-myai

The original project plan doc still had `tpai` everywhere (old working name). Rewrote `plans/2026-03-11-tpai-system/plan.md` entirely — replaced all `tpai` refs, updated file structure to match reality, marked all 9 phases complete. The doc is now an accurate historical record of the build.

---

### update-runtime-docs

`GEMINI.md` and `AGENTS.md` both had a duplicate `debugger` entry and were missing 6 of the 13 agents. Fixed both with the full list: brainstormer, codebase-mapper, code-reviewer, debugger, docs-manager, fullstack-developer, git-manager, planner, researcher, roadmapper, tester, ui-ux-designer, verifier. Also added `/myai:verify` to `CLAUDE.md` commands table and README with pre-build/post-build labels to distinguish validate vs verify.

---

### plan-phase-format

Researched CCS's phase file format (`plan/hard.md` + `planning` skill). Decision: **keep myai's current single-file format for task and phase plans**. CCS's per-phase files are heavyweight (12 sections each) and designed for a different workflow. myai already uses phase files for parallel plans — that's the right scope. No changes made to code.

---

### brainstorm-improvements

Ran brainstorm session on 3 workflow gaps:

1. **Post-plan validation offer** — `/myai:plan` should offer `/myai:validate` at the end for phase/parallel plans (like CCS does). Created task plan `2026-03-12-post-plan-validate`.

2. **Stale plan detection** — `/myai:progress` should flag plans with `PROGRESS.md` older than 7 days without `SUMMARY.md`. Created task plan `2026-03-12-progress-stale-detection`.

3. **`/myai:scout` command** — deferred. YAGNI. `/myai:plan` already scouts internally.
